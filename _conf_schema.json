{
    "exporter_api_url": {
        "description": "wechat-article-exporter API地址",
        "type": "string",
        "default": "https://localhost:3000",
        "hint": "wechat-article-exporter API地址,最后不需要斜杠，例如：http://localhost:3000"
    },
    "exporter_auth_key": {
        "description": "API认证密钥(auth-key)",
        "type": "string",
        "default": "",
        "hint": "API认证密钥(auth-key)"
    },
    "headers": {
        "description": "请求头信息",
        "type": "string",
        "hint": "需要自行获取binds接口请求头信息，可以直接以字典形式复制过来，注意:去除content-length字段",
        "default": "{}"
    },
    "encryption_secret_key": {
        "description": "账号数据加密密钥",
        "type": "string",
        "hint": "用于加密本地保存的手机号和角色ID。首次使用请填写一段固定随机字符串；修改后旧绑定数据将无法解密。",
        "default": ""
    }
}